🔬 ЛАБОРАТОРНАЯ РАБОТА №1: Реализация кольцевого дека

### Цель работы
Реализовать дек на кольцевом массиве и уверенно объяснять формулу индексации.

### Теоретическая справка (кратко)
Кольцевой массив мыслится как цикл: после последнего элемента идёт первый.

### Пошаговые инструкции выполнения
1. Создай массив `buf[reserved]`.
2. Храни `size` и `offset`.
3. Реализуй:
    - `push_back(x)`, `push_front(x)`,
    - `pop_back()`, `pop_front()`,
    - `get(i)` (доступ по индексу).
4. При переполнении увеличивай `reserved` (например, в 2 раза) и “распрямляй” элементы в новый буфер.

### Необходимые формулы и данные
- `pos(i) = (offset + i) % reserved`
- “последний” элемент: `pos(size-1)`
- для `push_front`: `offset = (offset - 1 + reserved) % reserved`

### Контрольные вопросы
1. Почему `get(i)` работает за O(1)?
2. Почему `pushFront/pushBack` тоже O(1) (амортизированно)?
3. Где чаще всего ломаются реализации кольца?