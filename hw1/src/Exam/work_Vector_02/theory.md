🔬 ЛАБОРАТОРНАЯ РАБОТА №1: Реализуем “чанковый динамический массив” и считаем память

### Цель работы
Понять, как работает двухуровневое хранение: как считается индекс, как выделяются чанки, и почему реаллокация “не тащит” все данные.

### Теоретическая справка (кратко)
Чанк фиксированного размера 512 байт содержит `chunkSize = 512/sizeof(T)` элементов. Верхний слой — массив ссылок на чанки.

### Пошаговые инструкции выполнения
1. Выбери тип `T` (для простоты `int`).
2. Посчитай `chunkSize`.
3. Опиши структуру:
    - `List<int[]> chunks;`
    - `int size;`
4. Реализуй:
    - `int get(int i)`
    - `void pushBack(int x)` (создаёт новый чанк, если нужен)
    - `void popBack()` (и “освобождает” чанк, если он стал пустым)
5. Проверь на примере: добавь 1, 2, 3, …, `chunkSize+2` элементов и убедись, что создаётся 2 чанка.

### Необходимые формулы и данные
- `chunkSize = 512 / sizeof(T)`
- `chunk = i / chunkSize`
- `off = i % chunkSize`

### Контрольные вопросы
1. Почему `get(i)` всё ещё O(1)?
2. Что именно может реаллоцироваться в двухуровневой схеме?
3. Когда можно безопасно освободить чанк?